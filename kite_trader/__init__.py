"""Run the TradingAgents pipeline over a watchlist and route ratings to Zerodha.

Upstream TradingAgents is consumed as an installed library and is never
modified; :mod:`kite_trader.agents` is the only module that imports it.
"""

__version__ = "0.1.0"
